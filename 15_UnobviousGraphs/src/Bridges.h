#pragma once
#include <map>
#include <list>
#include <string>

class Bridges {
public:
    static bool isProjectSuccess(const std::map<std::string, std::list<std::string>>& project) {
        return false;
    }
};
